#include <C:\Users\guilh\Desktop\Nuvem\RTOS\Pesquisa-Macnica-DHW\curl\curl.h>
#include <stdio.h>
#include <stdlib.h>

int main(void) {

    // Inicializando curl
    // CURL *curl = curl_easy_init();

    // // Verificando se curl inicializou
    // if(!curl){
    //     fprintf(stderr, "init failed\n");
    //     return EXIT_FAILURE;
    // }

    // //setando as opções
    // curl_easy_setopt(curl, CURLOPT_URL, "htttp://jacobsorber.com");


    // //perferm out action
    // CURLcode result = curl_easy_perform(curl);
    // if (result != CURLE_OK) {
    //     fprintf(stderr, "download problem : %s\n", curl_easy_strerror(result));
    // }


    // curl_easy_cleanup(curl);
    // return EXIT_SUCCESS;
}